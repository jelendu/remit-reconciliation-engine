# App runtime (Streamlit Community Cloud installs this file)
streamlit>=1.36,<2
duckdb>=1.0,<2
pandas>=2.1,<3
pyarrow>=15,<21
altair>=5,<6

# Pipeline / CI (dbt transforms; harmless for the dashboard runtime)
dbt-duckdb>=1.8,<2
