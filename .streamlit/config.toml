# Designed light theme — the app renders identically for every visitor
# (status-tinted table rows are specified against this surface).
[theme]
base = "light"
primaryColor = "#2a78d6"
backgroundColor = "#fcfcfb"
secondaryBackgroundColor = "#f2f1ec"
textColor = "#0b0b0b"

[server]
headless = true
